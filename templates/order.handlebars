<head>
    <style>
        table {
            background-image: url('https://assets.bigcartel.com/theme_images/48216620/BIG.png?auto=format&fit=max&h=200&w=200');
            background-repeat: repeat;
            background-size: 220px 220px;
            width: 100%;
            border-radius: .75rem;
        }

        h1, p, b, a, a:link, a:visited, a:hover, a:active, li {
            color: #783c55;
        }

        p, b, a, li, blockquote {
            font-size: 1.2rem;
        }

        b.address {
            font-size: 1.1em;
        }

        p {
            width: 100%;
        }

        blockquote {
            color: #bb3f73;
            background-color: #fca7bc50;;
            padding: .8rem;
            border-radius: .4rem;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <table width="100%" border="0" cellspacing="0" cellpadding="20"><tr><td>
        <p>New order via azulilah website</p>
        <p>
            <b>Email: <a href="mailto:{{Email}}">{{Email}}</a></b> <br/>
            <b>Paypal Reference: {{PaypalReference}}</b> <br/>
            <b>Order Total: ${{TotalPrice}}</b> <br/>
            <b>Item Total: ${{ItemTotal}}</b> <br/>
            <b>Shipping Total: ${{ShippingTotal}}</b> <br/>
            <b style="margin-top: 5px">Items: </b>
            <ul style="margin-top: 0px; padding-top: 0px;">
                {{#each Items}}
                    <li>{{this.Name}} x{{this.Quantity}} - ${{this.Price}}</li>
                {{/each}}
            </ul>
            <b style="margin-top: 5px">Shipping Address: </b> <br/>
            <b class="address">{{Name}}</b> <br />
            <b class="address">{{Line1}}</b> <br/>
            {{#if Line2}}<b class="address">{{Line2}}</b> <br/>{{/if}}
            <b class="address">{{City}}</b> <br/>
            <b class="address">{{State}}</b> <br/>
            <b class="address">{{Zip}}</b> <br/>
            <b class="address">{{Country}}</b> <br/>
        </p>
    </td></tr></table>
</body>
